services:
  queenbee:
    container_name: queenbee
    image: queenbee
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8000:3000'
    env_file:
      - .env
    mem_limit: 1g
    cpus: "2.0"
