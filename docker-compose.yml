services:
    queenbee:
        container_name: queenbee
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '8050:3000'
        env_file:
            - .env
