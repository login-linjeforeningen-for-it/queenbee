name: CI Pipeline

on:
  push:
    branches: ["dev", "main"]
  pull_request:
    branches: ["dev", "main"]

jobs:
  call-common-ci:
    uses: Login-Linjeforening-for-IT/.github/.github/workflows/common-ci.yml@main