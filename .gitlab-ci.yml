---
stages:
  - build
  - trigger-stages

build:
  stage: build
  image:
    name: gcr.io/kaniko-project/executor:v1.20.1-debug
    entrypoint: [""]
  script:
    - echo $CI_COMMIT_TAG
    - |
      if [[ "$CI_COMMIT_TAG" =~ -dev$ ]]; then
        BASE_URL="https://queenbee-api-dev.login.no/v1"
        echo "Building in development environment"
      else
        BUILD_ENV="https://queenbee-api.login.no/v1"
        echo "Building in production environment"
      fi
    - /kaniko/executor
      --context "${CI_PROJECT_DIR}"
      --dockerfile "${CI_PROJECT_DIR}/Dockerfile"
      --destination "${CI_REGISTRY_IMAGE}:${CI_COMMIT_TAG}"
      --build-arg BASE_URL=$BASE_URL
  rules:
    - if: '($CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "master" || $CI_COMMIT_BRANCH == "dev")'

trigger-stages:
  stage: trigger-stages
  trigger:
    project: tekkom/infrastructure/mend-renovate/mend-renovate-bot
    strategy: depend
    branch: main
  rules:
    # Only triggering stage when a new image is built.
    - if: '($CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "master") && $CI_COMMIT_TAG =~ /^\d+\.\d+\.\d+$/'
    - if: '$CI_COMMIT_BRANCH == "dev" && $CI_COMMIT_TAG =~ /^\d+\.\d+\.\d+-dev$/'
