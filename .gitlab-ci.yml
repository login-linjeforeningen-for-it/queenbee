---
stages:
  - build
  - trigger-stages

build:
  stage: build
  image:
    name: gcr.io/kaniko-project/executor:v1.20.1-debug
    entrypoint: [""]
  script:
    - |
      if [[ "$CI_COMMIT_TAG" =~ -dev$ ]]; then
        BASE_URL="https://queenbee-api-dev.login.no/v1"
        TYPE="testgitlab"
        PROJECT_ID="testgitlab"
        PRIVATE_KEY_ID="testgitlab"
        PRIVATE_KEY="testgitlab"
        CLIENT_EMAIL="testgitlab"
        CLIENT_ID="testgitlab"
        AUTH_URI="testgitlab"
        TOKEN_URI="testgitlab"
        AUTH_CERT_URL="testgitlab"
        CLIENT_CERT_URL="testgitlab"
        UNIVERSE_DOMAIN="testgitlab"
        GITLAB_MESSAGE="This variable is defined in the Gitlab Pipeline editor for QueenBee. Whatever is missing must be audited there."
        echo "Building in development environment"
      else
        BUILD_ENV="https://queenbee-api.login.no/v1"
        echo "Building in production environment"
      fi
    - /kaniko/executor
      --context "${CI_PROJECT_DIR}"
      --dockerfile "${CI_PROJECT_DIR}/Dockerfile"
      --build-arg "IMAGE_VERSION=${CI_COMMIT_TAG}"
      --destination "${CI_REGISTRY_IMAGE}:${CI_COMMIT_TAG}"
  rules:
    - if: '$CI_COMMIT_TAG =~ /^\d+\.\d+\.\d+$/'
    - if: '$CI_COMMIT_TAG =~ /^\d+\.\d+\.\d+-dev$/'

trigger-stages:
  stage: trigger-stages
  trigger:
    project: tekkom/infrastructure/mend-renovate/mend-renovate-bot
    strategy: depend
    branch: main
  rules:
    - if: '$CI_COMMIT_TAG =~ /^\d+\.\d+\.\d+$/'
    - if: '$CI_COMMIT_TAG =~ /^\d+\.\d+\.\d+-dev$/'
